mixin input(name,type)
    div(class="flex flex-col mt-5")        
        label(for=name class='uppercase tracking-wider')= name
        input(type=type name=name id=name class='border-b border-green-500')